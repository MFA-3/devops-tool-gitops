apiVersion: v1
kind: Service
metadata:
  name: cloud-frontend
  namespace: dev
spec:
  type: NodePort
  selector:
    app: cloud-frontend
  ports:
  - port: 4300
    targetPort: 4300
    nodePort: 30430
